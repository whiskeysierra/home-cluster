apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dns

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: unbound-config
    files:
      - unbound.conf
